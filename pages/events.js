import Calendar from '../components/Calendar';
import Layout from '../components/Layout';

const events = () => {
  return (
    <Layout title="Events">
      <div>
        <Calendar />
      </div>
    </Layout>
  );
};
export default events;
